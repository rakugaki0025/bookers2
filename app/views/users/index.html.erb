  <!-- Users_一覧_#index_top_bookers2_views_users_index_完了-->
  
  <!-- ユーザーの投稿一覧 -->
  
<html>
  <p id="notice"></p>
  
  <div class="container px-5 px-sm-0">
    <div class="row">
      
        <!-- 部分テンプレートrender表記 -->
      <div class="col-md-3">
        <%= render 'users/list', user: @user,book: @book %>
      </div>
      
      <div class="col-md-8 offset-md-1">
        <h1>Users</h1>
        
        <table class="table table-hover table-inverse">
          <thead>
            <tr>
              <th>image</th>
              <th></th>
              <th>name</th>
              <th colspan="3"></th>
            </tr>
          </thead>
            <!-- image_name -->
          <tbody>
          
            <% @users.each do |user| %>
             
              <tr align="center">
                
                <td>
                  <%= image_tag user.get_profile_image(100,100) %>
                </td>
                  <!-- user_画像 -->
                <td></td>
                
                <td>
                  <%= user.name %>
                </td>
                  <!-- user_name -->
                
                <td>
                  <%= link_to "show",user_path(user.id) %>
                </td>
                  <!-- リンク -->
                
              </tr>
            <% end %>
          
          </tbody>
            <!-- User 一覧 -->
        </table>
        
      </div>
    </div>
  </div>
</html>

